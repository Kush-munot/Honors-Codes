import React from 'react'

const Children = (props) => {
    return (
        <div>
            <h3>{props.fname}</h3>
            <h4>{props.lname}</h4>
        </div>
    )
}

export default Children
